% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numerical_stability_linter.R
\name{numerical_stability_linter}
\alias{numerical_stability_linter}
\title{Prefer numerically stable functions}
\usage{
numerical_stability_linter(
  assume_real_numeric = TRUE,
  include_heuristics = FALSE
)
}
\arguments{
\item{assume_real_numeric}{Logical, default \code{TRUE}. Assume that unknown
inputs to arithmetic, \code{log()}, and \code{exp()} are ordinary real numeric
vectors. Set to \code{FALSE} to disable the \code{log1p()}, \code{expm1()}, \code{plogis()},
and \code{qlogis()} rules. Explicit complex literals are always excluded from
these rules. This is relevant to R versions before 4.6.0, where \code{log1p()}
and \code{expm1()} do not support complex inputs, and to class-specific methods.}

\item{include_heuristics}{Logical, default \code{FALSE}. Also report log-sum-exp
expressions and direct logarithms of \code{gamma()}, \code{factorial()}, and
\code{choose()}. These hints require reviewing the domain of the inputs;
their logarithmic counterparts compute logarithms of absolute values.}
}
\description{
Detect expressions that lose accuracy through cancellation, underflow, or
overflow and recommend specialized functions. The linted code is never
evaluated. Suggestions are warnings, not automatic edits.
}
\section{Recognized replacements}{

\itemize{
\item \code{log(1 + x)}, \code{log(x + 1)}, and \code{log(1 - x)} suggest \code{log1p()}.
\item \code{exp(x) - 1} and \code{1 - exp(x)} suggest \code{expm1()}.
\item \code{1 / (1 + exp(-x))} and \code{exp(x) / (1 + exp(x))} suggest \code{stats::plogis()}.
\item \code{log(p / (1 - p))} suggests \code{stats::qlogis()}.
\item \code{log(beta(a, b))} suggests \code{lbeta()}. Logarithms of absolute values of
\code{gamma()}, \code{factorial()}, and \code{choose()} suggest their logarithmic APIs.
\item \code{log(dnorm(x))} and analogous standard stats density calls suggest
\code{log = TRUE}; distribution functions suggest \code{log.p = TRUE}.
\item \code{1 - pnorm(x)} suggests the opposite \code{lower.tail} setting.
\code{log(1 - pnorm(x))} combines this with \code{log.p = TRUE}.
\item \code{qnorm(1 - p)} and analogous quantile calls suggest the opposite tail.
}
}

\section{Scope}{

Unqualified calls are assumed to refer to the standard base/stats functions.
Explicit calls from other namespaces are ignored. Local function masking and
S3/S4 dispatch cannot be resolved statically. Named, partially named, and
positional arguments are matched against the standard function signatures.
Calls with missing arguments, \code{...}, or unknown log/tail flags are skipped.
Non-natural logarithms are skipped. Rewrites removing repeated evaluations
are restricted to repeated symbols or numeric constants.
}

\examples{
# will produce lints
lint(text = "log(1 + x)", linters = numerical_stability_linter())
lint(text = "exp(x) - 1", linters = numerical_stability_linter())
lint(text = "log(1 - pnorm(x))", linters = numerical_stability_linter())

# okay
lint(text = "log1p(x)", linters = numerical_stability_linter())
lint(text = "pnorm(x, lower.tail = FALSE, log.p = TRUE)", linters = numerical_stability_linter())

}
\seealso{
\link{linters} for a complete list of linters available in lintr.
}
\section{Tags}{
\link[=best_practices_linters]{best_practices}, \link[=configurable_linters]{configurable}, \link[=robustness_linters]{robustness}
}
