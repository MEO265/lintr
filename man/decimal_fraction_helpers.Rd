% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decimal_fraction_linter.R
\name{is_exact_binary_literal}
\alias{is_exact_binary_literal}
\alias{decimal_parts_with_exp}
\alias{decimal_parts_to_fraction}
\alias{reduce_fraction}
\alias{integer_gcd}
\title{Determine if a decimal literal is exactly representable in binary}
\usage{
is_exact_binary_literal(fractional_part)

decimal_parts_with_exp(number)

decimal_parts_to_fraction(integer_part, fractional_part)

reduce_fraction(numerator, denominator)

integer_gcd(a, b)
}
\arguments{
\item{fractional_part}{Character scalar fractional part.}

\item{number}{Character scalar decimal literal.}

\item{integer_part}{Character scalar integer part.}

\item{numerator}{Integer numerator.}

\item{denominator}{Integer denominator.}

\item{a}{Integer.}

\item{b}{Integer.}
}
\value{
\code{TRUE} when the literal is exactly representable in binary.

A list with \code{integer_part} and \code{fractional_part}, or \code{NULL} if invalid.

A list with \code{numerator} and \code{denominator}, or \code{NULL} if invalid.

A reduced fraction list or \code{NULL} if invalid.

Integer greatest common divisor.
}
\description{
Determine if a decimal literal is exactly representable in binary

Extract decimal literal parts with exponent applied

Convert decimal literal parts to a reduced fraction

Normalize a fraction into reduced integer form

Compute the greatest common divisor for integers
}
\keyword{internal}
